server:
  port: 8086

spring:
  application:
    name: tournament-service
  datasource:
    url: jdbc:postgresql://localhost:5432/bitpub_db
    username: bitpub
    password: bitpub_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
