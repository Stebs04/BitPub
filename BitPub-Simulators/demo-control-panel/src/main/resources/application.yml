# autore Timothy Giolito 20054431
# Configurazione applicativa principale per il pannello di controllo demo.
server:
  port: 8085

spring:
  application:
    name: demo-control-panel
  jackson:
    serialization:
      write-dates-as-timestamps: false

mqtt:
  broker:
    url: tcp://localhost:1883
  client-id: demo-control-panel-publisher
